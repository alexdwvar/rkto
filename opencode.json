{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "command": {
    "dev": {
      "description": "Start the anime API dev server",
      "prompt": "Run the dev server with hot reload using bun"
    },
    "test": {
      "description": "Run tests with bun test",
      "prompt": "Run all tests using bun test"
    },
    "test:watch": {
      "description": "Run tests in watch mode",
      "prompt": "Run tests in watch mode using bun test --watch"
    },
    "db:generate": {
      "description": "Generate Drizzle migrations",
      "prompt": "Generate Drizzle schema migrations"
    },
    "db:migrate": {
      "description": "Run Drizzle migrations",
      "prompt": "Run pending Drizzle migrations"
    },
    "db:studio": {
      "description": "Open Drizzle Studio",
      "prompt": "Open Drizzle Studio to inspect the database"
    }
  }
}